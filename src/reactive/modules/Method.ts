import { elBindMethod } from "../../Method";
import { TPluginItem } from "../../types/Plugin";
import { IProperties } from "../../types/Properties";

export default {
  collectElAttrRef(attrItem, properties: IProperties): void {
    // console.log(attrItem);
    
    // elBindMethod(target, properties, properties);
  }
} as TPluginItem